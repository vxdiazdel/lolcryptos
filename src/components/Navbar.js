import React from "react";

const Navbar = () => ( 
    <nav className="navbar">
        <a href="/" className="navbar__logo">
            LOLCryptos<span role="img" aria-label="rocket" className="navbar__emoji">🚀</span>
        </a> 
    </nav>
);

export default Navbar;